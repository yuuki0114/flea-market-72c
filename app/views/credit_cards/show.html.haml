= render partial: "items/header"
= render partial: "items/menu"
.mypage
  .wrapper
    = render partial: "users/side_bar"
    %section.credit-card-content
      %h2.credit-card-content--head
        クレジット情報の確認
      .credit-card-content--form
        .form-group
          クレジットカード一覧
          .form-group--credit_logo
            = image_tag "/#{@card_src}", alt: @card_info.brand, width:"40", height: "25"
          .form-group--card-number
            .card-number
              = "**** **** **** " + @card_info.last4
            .card__body__main__data
              = @card_info.exp_month.to_s + " / " + @card_info.exp_year.to_s.slice(2,3) 
          .delete-btn
            = link_to "削除する", credit_card_path, method: :delete, class: "delete-btn"


      -# .card__body__main
      -#   .card__body__main__title
      -#     Your Credit Card
      -#   .card__body__main__data
      -#     = "**** **** **** " + @card_info.last4
      -#   .card__body__main__data
      -#     = @card_info.exp_month.to_s + "月 / " + @card_info.exp_year.to_s.slice(2,3) + "年"
      -#   .card__body__main__brand
      -#     = image_tag "/#{@card_src}", alt: @card_info.brand
      -# .delete_btn
      -#   = link_to credit_card_path, method: :delete, class: "submit-btn" do
      -#     クレジット情報を削除

    
= render partial: "items/footer"
= render partial: "items/post-btn"